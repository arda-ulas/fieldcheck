# Handoff

Read this first when starting a fresh session. Spec lives outside this repo (career-os); the
summary of what matters is in `docs/stories.md` and `AGENTS.md`.

## Current state
- Block 0 in progress: repo created, secret scanning on, docs written. Waiting for dotnet/Docker installs.

## Next step
- Solution skeleton (`src/FieldCheck.Api`, `tests/FieldCheck.UnitTests`, `tests/FieldCheck.IntegrationTests`) + CI green → checkpoint (a).

## Open issues
- None yet.
