# Teardown record

Resource group `rg-fieldcheck` (App Service `app-fieldcheck-dcb438`, Azure SQL `sql-fieldcheck-dcb438`/`sqldb-fieldcheck`,
storage `stfieldcheckdcb438`) was deleted on **2026-09-14T20:02:47Z** with `az group delete -n rg-fieldcheck --yes`.
`az group exists` returned `false` afterwards and the live URL stopped resolving.

The deployment was live from 2026-09-14 (creation) to 2026-09-14T20:02:47Z. The smoke-test evidence captured while it
was live is in `smoke-live-2026-09-14.md`.
